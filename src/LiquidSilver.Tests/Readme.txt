Before running the tests, ensure you have checked the followings:
1. Ensure the TestSettings.SiteUrl value is correct according to your farm
   configuration. A SharePoint site in that URL should be accessible by the
   user account that runs the tests.
